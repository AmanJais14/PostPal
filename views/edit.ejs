<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/Stylesheets/style.css">
</head>
<body>
    <div class = "w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex w-full items-end justify-end">
            <a class ="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">logout</a>
        </div>
        <h5 class="mb-5">Edit your post</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" placeholder="What's on your mind?" name="content" id=""><%= post.content %>
            </textarea>
            <input class="px-3 py-2 bg-yellow-500 text-black block rounded-md mt-2 w-41 text-sm" type="submit" value="Update Post">
        </form>
        
    </div>
</body>
</html> -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- <link rel="stylesheet" href="/Stylesheets/style.css"> -->
</head>
<body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center p-5">
    <div class="w-full max-w-lg">
        <div class="flex items-end justify-end">
            <a class="bg-red-500 hover:bg-red-600 transition-colors rounded-md px-4 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
        </div>
        <h5 class="text-xl font-semibold mb-5">Edit your post</h5>
        <form action="/update/<%= post._id %>" method="post" class="space-y-4">
            <textarea class="w-full resize-none bg-zinc-800 border-2 border-zinc-700 rounded-md p-3 text-sm focus:outline-none focus:ring-2 focus:ring-yellow-500" placeholder="What's on your mind?" name="content" id="" rows="5"><%= post.content %></textarea>
            <input class="w-full bg-yellow-500 hover:bg-yellow-600 transition-colors text-black font-semibold px-4 py-2 rounded-md cursor-pointer" type="submit" value="Update Post">
        </form>
    </div>
</body>
</html>
