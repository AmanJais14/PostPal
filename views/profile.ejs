<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/Stylesheets/style.css">
</head>
<body>
    <div class = "w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex w-full items-end justify-end">
            <a class ="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">logout</a>
        </div>
        <h3 class="text-3xl mb-1"><span class="font-light">Hello</span>, <%= user.name %> </h3>
        <h5 class="mb-5">you can create a new post</h5>
        <form action="/post" method="post">
            <textarea class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" placeholder="What's on your mind?" name="content" id=""></textarea>
            <input class="px-3 py-2 bg-blue-500 block rounded-md mt-2 w-41 text-sm" type="submit" value="Create New Post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400"> Your Posts.</h3>
            <div class="postcontainer  mt-5">
                <% user.posts.reverse().forEach(function(post) { %>
                    <div class="post mb-2 w-1/3 p-5 border-[1px] rounded-md border-zinc-800">
                        <h4 class="text-blue-500 mb-4"><%= user.email %></h4>
                        <p class="tracking-tight text-sm"><%= post.content %></p>
                        <small class="mt-1 inline-block" ><%= post.likes.length %> likes</small>
                        <div class="btns flex gap-5 mt-3">
                            <a class = "text-blue-500 " href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) === -1? "Like" : "Unlike" %>
                            </a>
                            <a class = "text-zinc-600" href="/edit/<%= post._id %>">edit</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html> -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- <link rel="stylesheet" href="/Stylesheets/style.css"> -->
</head>
<body class="bg-zinc-900 text-white min-h-screen p-10">
    <div class="w-full max-w-4xl mx-auto">
        <div class="flex justify-end mb-5">
            <a class="bg-red-500 hover:bg-red-600 transition-colors rounded-full px-4 py-2 text-sm text-white" href="/logout">Logout</a>
        </div>
        <div class="bg-zinc-800 shadow-md rounded-lg p-6 mb-5">
            <h3 class="text-3xl mb-1"><span class="font-light">Hello</span>, <%= user.name %></h3>
            <h5 class="text-gray-400 mb-5">You can create a new post</h5>
            <form action="/post" method="post" class="space-y-4">
                <textarea class="w-full resize-none bg-zinc-700 border border-zinc-600 rounded-md p-3 text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="What's on your mind?" name="content" id=""></textarea>
                <input class="w-full px-5 py-2 rounded-full bg-blue-500 hover:bg-blue-600 transition-colors text-white font-semibold cursor-pointer" type="submit" value="Create New Post">
            </form>
        </div>
        <div class="posts">
            <h3 class="text-gray-400 mb-5">Your Posts</h3>
            <div class="postcontainer space-y-4">
                <% user.posts.reverse().forEach(function(post) { %>
                    <div class="post bg-zinc-800 shadow-md rounded-lg p-6 border border-zinc-700 text-white">
                        <h4 class="text-blue-500 mb-4"><%= user.email %></h4>
                        <p class="tracking-tight text-gray-300 text-sm mb-4"><%= post.content %></p>
                        <small class="inline-block text-gray-500 mb-4"><%= post.likes.length %> likes</small>
                        <div class="btns flex gap-5">
                            <a class="text-blue-500 hover:underline" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-gray-500 hover:underline" href="/edit/<%= post._id %>">Edit</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>

